[Unit]
# Override condition to allow Dropbear to start even without authorized_keys
ConditionFileNotEmpty=

[Service]
# Allow password login and auto-generate host keys
ExecStart=
ExecStart=/usr/sbin/dropbear -F -E -R
