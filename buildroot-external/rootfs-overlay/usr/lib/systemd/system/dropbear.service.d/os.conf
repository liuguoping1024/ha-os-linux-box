[Unit]
RequiresMountsFor=/etc/dropbear
# ConditionFileNotEmpty=/root/.ssh/authorized_keys  # Disabled to allow password login

[Service]
Type=forking
PIDFile=/run/dropbear.pid
ExecStartPre=
ExecStart=
ExecStart=/usr/sbin/dropbear -E -R -p 22
KillMode=mixed
OOMPolicy=continue
